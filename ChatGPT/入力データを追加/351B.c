コード生成要件
・このコードをC言語で作ってください。
・問題の内容を以下にまとめます。
縦Nマス、横Nマスのグリッドが2個与えられます。それぞれグリッドA,グリッドBと呼びます。
グリッドの各マスには英小文字が書かれています。
グリッドAの上からi行目、左からj列目に書かれている文字はA(i,j)です。
グリッドBの上からi行目、左からj列目に書かれている文字はB(i,j)です。
二つのグリッドは1か所だけ書かれている文字が異なります。すなわちA(i,j)=B(i,j)ではないN以下の正整数の組(i,j)はちょうど1個存在します。
個の(i,j)を求めてください。
・次に問題の制約を以下にまとめます。
1<=N<=100
A(i,j),B(i,j)は全て英小文字
A(i,j)=B(i,j)ではない(i,j)がちょうど一個存在する
・入力フォーマット
A(1,1) A(1,2) ・・・・・・・・A(1,N)
A(2,1) A(2,2) ・・・・・・・・A(2,N)
・
・
・
・
A(N,1) A(N,2) ・・・・・・・・A(N,N)
B(1,1) B(1,2)　・・・・・・・B(1,N)
B(2,1) B(2,2)　・・・・・・・B(2,N)
・
・
・
・
・
・
B(N,1)B(N,2)・・・・・・・・B(N,N)
・出力フォーマット
A(1,j)=B(i,j)ではないN以下の正整数の組を(i,j)とする。この時(i,j)を以下の形式で出力せよ
i j
処理例
例えば1行目に3
続いて
a b c
d e f
g h i
a b c
b e f
g h i
と出力されたとする。
この中でグリッドA(1行目から3行目),グリッドB(4行目から6行目)を比べて異なるものは、グリッドAの2行目(2行目)1番目のdとグリッドBの2行目(5行目)1番目のbが異なっている。
よって、2 1を出力します。
では問題をとき、コードを生成してください
正解
#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    char grid_A[100][101], grid_B[100][101];

    // グリッドAの入力
    for (int i = 0; i < N; i++) {
        scanf("%s", grid_A[i]);
    }

    // グリッドBの入力
    for (int i = 0; i < N; i++) {
        scanf("%s", grid_B[i]);
    }

    // グリッドAとグリッドBを比較して異なる箇所を探す
    int diff_row = -1, diff_col = -1;
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < N; j++) {
            if (grid_A[i][j] != grid_B[i][j]) {
                diff_row = i + 1;
                diff_col = j + 1;
                break;
            }
        }
        if (diff_row != -1) {
            break;
        }
    }

    // 結果を出力
    printf("%d %d\n", diff_row, diff_col);

    return 0;
}
