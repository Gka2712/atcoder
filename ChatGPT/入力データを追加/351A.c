コード生成要件
・このコードをC言語で作ってください。
・問題の内容を以下にまとめます。
チーム高橋とチーム青木が、チーム高橋を専攻として野球を行っています。現在、9回表までが終了し、9回裏が始まろうとしています。
試合においてチーム高橋はi回表にAi点を取り、チーム青木はj回裏にBj点取りました。
個々で、9かい表の終了時点でチーム高橋の得点はチーム青木の得点以上です。
チーム青木は9回裏に最低何点取れば勝利することができるか求めてください。
ただし、9かい裏の終了時点で同点で合った場合は引き分けとなり、すなわちチーム青木が勝利するためには9回裏の終了時点でチーム高橋より真に多くの点を取っている必要があるものとします。
なお、(ある時点における)チーム高橋の得点はそれまでの回の表に取った点数の合計であり、チーム青木の得点はそれまでの回の裏に取った点数の合計です。
・次に問題の制約を以下にまとめます。
0<=Ai,Bi<=99
A1+A2+A3+A4+A5+A6+A7+A8+A9>=B1+B2+B3+B4+B5+B6+B7+B8
入力は全て整数
入力フォーマット
入力は以下の形式で標準入力が与えられる。
A1 A2 A3 A4 A5 A6 A7 A8 A9 
B1 B2 B3 B4 B5 B6 B7 B8
1行目にチーム高橋の点数A1~A9点をそれぞれ入力、2行目にチーム青木の点数B1~B8点を入力
出力フォーマット
チーム青木が勝利するために9回裏に取る必要のある最小の得点を出力せよ
処理例
例えば、1行目で「0 1 0 1 2 2 0 0 1」、2行目で「1 1 0 0 0 0 1 0」が与えられた場合、9回表の終了時点でチーム高橋の得点は、7点(0+1+0+1+2+2+0+0+1)チーム青木の得点は、3点(1+1+0+0+0+0+1+0)
これより、チーム青木は、9回裏に5点取れば、7-8で、チーム青木が勝利することができます。
では、以下の問題を解いてください。
不正解
#include <stdio.h>

int main() {
    int A[9], B[8];
    
    // チーム高橋の得点を入力
    for (int i = 0; i < 9; i++) {
        scanf("%d", &A[i]);
    }
    
    // チーム青木の得点を入力
    for (int i = 0; i < 8; i++) {
        scanf("%d", &B[i]);
    }

    // 9回表の得点と8回裏の得点を比較して、チーム高橋の得点を計算
    int sum_A = 0;
    for (int i = 0; i < 9; i++) {
        sum_A += A[i];
    }

    // 8回裏までのチーム青木の得点
    int sum_B = 0;
    for (int i = 0; i < 8; i++) {
        sum_B += B[i];
    }

    // 9回裏に取る必要のある最小の得点を計算
    int required_score = sum_A - sum_B;

    // 出力
    printf("%d\n", required_score);

    return 0;
}
/*
不正解
理解漏れ チーム青木が勝つための条件は、チーム高橋の得点-チーム青木の得点+1なのに対し、チーム高橋の得点-チーム青木の得点で計算されていた。
だんだんと近づいてきている。
#include <stdio.h>

int main() {
    int A[9], B[8];
    
    // チーム高橋の得点を入力
    for (int i = 0; i < 9; i++) {
        scanf("%d", &A[i]);
    }
    
    // チーム青木の得点を入力
    for (int i = 0; i < 8; i++) {
        scanf("%d", &B[i]);
    }

    // 9回表の得点と8回裏の得点を比較して、チーム高橋の得点を計算
    int sum_A = 0;
    for (int i = 0; i < 9; i++) {
        sum_A += A[i];
    }

    // 8回裏までのチーム青木の得点
    int sum_B = 0;
    for (int i = 0; i < 8; i++) {
        sum_B += B[i];
    }

    // 9回裏に取る必要のある最小の得点を計算
    int required_score = sum_A - sum_B+1;

    // 出力
    printf("%d\n", required_score);

    return 0;
}
*/
